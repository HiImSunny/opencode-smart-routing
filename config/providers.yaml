providers:
  ollama:
    base_url: "http://localhost:11434"
    api_key: ""
    type: ollama

  opencode_go:
    base_url: "http://localhost:8080/v1" # Ví dụ port của OpenCode Go
    api_key_env: "" # Cấu hình trống nếu chạy local không cần key
    type: openai_compatible
